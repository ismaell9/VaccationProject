package vaccinationsystem;

/**
 *
 * 
 */
public class Admin extends Staff{

/**
 * access modifier is changeable 
 * remember it's better to stay "private"
 * @return true if added, false if not
 * return type might be changed to void
 */
    public boolean addAdmin(){
        return true;
    }

/**
 * access modifier is changeable 
 * remember it's better to stay "private"
 * @return true if added, false if not
 * return type might be changed to void
 */
    public boolean addDoctor(){
        return true;
    }

/**
 * access modifier is changeable 
 * remember it's better to stay "private"
 * @return true if added, false if not
 * return type might be changed to void
 */
    public boolean addStoreKeeper(){
        return true;
    }

/**
 * access modifier is changeable 
 * remember it's better to stay "private"
 * @return true if added, false if not
 * return type might be changed to void
 */
    public boolean addReservOfficer(){
        return true;
    }

/**
 * access modifier is changeable 
 * remember it's better to stay "private"
 * @return true if deleted, false if not
 * return type might be changed to void
 */
    public boolean deleteAdmin(){
        return true;
    }

/**
 * access modifier is changeable 
 * remember it's better to stay "private"
 * @return true if deleted, false if not
 * return type might be changed to void
 */
    public boolean deleteDoctor(){
        return true;
    }

/**
 * access modifier is changeable 
 * remember it's better to stay "private"
 * @return true if deleted, false if not
 * return type might be changed to void
 */
    public boolean deleteStoreKeeper(){
        return true;
    }

/**
 * access modifier is changeable 
 * remember it's better to stay "private"
 * @return true if deleted, false if not
 * return type might be changed to void
 */
    public boolean deleteReservOfficer(){
        return true;
    }
    
}
